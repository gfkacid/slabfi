# CRE workflow secrets (see Chainlink CRE docs). For local simulation, matching env vars
# are often injected automatically.
#
# Slab.Finance backend `/cards/.../price` expects header x-api-key = SLABFI_API_KEY.
# Map a workflow secret name to an env var the runner provides, e.g.:
# SLABFI_API_KEY_VAR: SLABFI_API_KEY

# Optional: declare secrets for authenticated EXTERNAL_PRICE_API calls.
# Local simulation: set matching env vars or use .env (see Chainlink CRE secrets docs).
# Example (rename keys to avoid CLI substring bugs per CRE known issues):
# EXTERNAL_PRICE_API_KEY_VAR: EXTERNAL_PRICE_API_KEY_SECRET
